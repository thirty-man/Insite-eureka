1. 페이지

// router 에서 파일 (jsx 또는 tsx) import
import { withAnalytics } from "./WithAnalytics";

// <App /> 컴포넌트를 withAnalytics로 감싸 전체 페이지를 추적하세요.
// <RoomList /> 컴포넌트만을 withAnalytics로 감싸 해당 페이지만을 추적하세요.

const routePaths: RouteObject[] = [
  {
    id: "app",
    path: "",
    element: withAnalytics(App),
    children: [
      {
        id: "roomlist",
        path: "",
        element: <RoomList />,
      },

        ...

2. 버튼

// 파일 (jsx 또는 tsx) import
import { traceButton } from "./Tracebutton";

// 타겟 버튼을 traceButton으로 감싸 해당 버튼의 클릭을 추적하세요.
<button onClick={() => traceButton("{버튼이름을 입력해주세요.}")}></button>;